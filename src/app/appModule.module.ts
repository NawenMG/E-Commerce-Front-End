/*import { CUSTOM_ELEMENTS_SCHEMA, NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { BrowserModule, provideClientHydration } from '@angular/platform-browser';//Per l'idratazione
import { AppComponent } from './app.component';
import { PComponent } from './p/p.component';



@NgModule({
  declarations: [ //Per dichiarare una lista di componenti
    AppComponent,
    PComponent
  ],
  imports: [ //Per le librerie
    BrowserModule, //Per usare le direttive
  ],
  providers: [ //Per l'injection
    provideClientHydration() //Funzione per l'idratazione del ssr
  ],
  //exports:[ //Per esportare componenti secondari
  //],
  bootstrap:[ //Dichiarazione del componente principale per l'app-root
    AppComponent
  ],

})
export class AppModule { }
*/
